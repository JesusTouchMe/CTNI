plugins {
    id 'java'
    id 'maven-publish'
}

group = 'cum.jesus.ctni'
version = '1.0'

repositories {
    mavenCentral()
}

dependencies {
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }

    repositories {
        maven {
            url = uri("file://C:/lib/ctni")
        }
    }
}